import 'package:flutter/material.dart';

class AppColors {
  // Institutional Ashram School Palette
  static const Color primary = Color(0xFF1A365D); // Deep Navy
  static const Color primaryLight = Color(0xFF2A4365);
  static const Color primaryDark = Color(0xFF0F172A);
  
  static const Color secondary = Color(0xFF276749); // Forest Green
  static const Color secondaryLight = Color(0xFF2F855A);
  
  static const Color accent = Color(0xFFD69E2E); // Ochre / Warm Amber
  static const Color accentLight = Color(0xFFECC94B);
  
  static const Color error = Color(0xFFE53E3E); // Crimson Red
  static const Color warning = Color(0xFFDD6B20); // Deep Orange
  static const Color success = Color(0xFF38A169); // Verdant Green
  static const Color info = Color(0xFF3182CE); // Sky Blue
  
  // Backgrounds and Surfaces
  static const Color background = Color(0xFFF7FAFC);
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFEDF2F7);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1A202C);
  static const Color textSecondary = Color(0xFF4A5568);
  static const Color textMuted = Color(0xFF718096);
  static const Color textWhite = Colors.white;
  
  // Borders
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderFocused = Color(0xFF3182CE);
}
